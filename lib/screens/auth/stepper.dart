import 'package:flutter/material.dart';

class SignupStepper extends StatelessWidget {
  const SignupStepper({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [Text("Daal rha hun matter, rukk ja")],
      ),
    );
  }
}
